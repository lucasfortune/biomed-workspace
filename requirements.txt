# Biomedical Image Segmentation Visualizer - Python Dependencies

# Core deep learning framework
# NOTE: Install PyTorch manually BEFORE running pip install -r requirements.txt
# This allows selecting the correct CUDA version for your system. Example:
#   pip3 install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
# See https://pytorch.org/get-started/locally/ for options.
# Minimum required: torch>=2.0.0, torchvision>=0.15.0, torchaudio>=2.0.0

# Image processing and data handling
tifffile>=2023.7.10
mrcfile>=1.5.0  # MRC import/export (file browser format conversion)
numpy>=1.21.0,<2.0.0
Pillow>=9.0.0,<11.0.0
scikit-image>=0.21.0  # For marching cubes mesh generation and denoising
PyWavelets>=1.4.0     # Required for skimage.restoration.estimate_sigma

# Scientific computing (often required for some PyTorch operations)
scipy>=1.9.0,<2.0.0

# For specific CUDA versions, uncomment and modify as needed:
# torch==2.1.0+cu118 -f https://download.pytorch.org/whl/torch_stable.html
# torchvision==0.16.0+cu118 -f https://download.pytorch.org/whl/torch_stable.html

# Development and debugging (optional)
matplotlib>=3.5.0  # For plotting training curves (optional)
tqdm>=4.64.0       # For progress bars (optional)

# Deep Learning Denoising (autoStructN2V, routed v1.0)
# The autoStructN2V v1.0 library is vendored in python/vendor/ (see
# python/vendor/VENDORED_VERSION) and automatically added to sys.path at
# runtime by python/denoising/__init__.py. No separate installation required.
tensorboard>=2.10.0  # Required for autoStructN2V training logging