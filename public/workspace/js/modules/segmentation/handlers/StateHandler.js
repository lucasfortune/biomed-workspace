/**
 * StateHandler.js - State Management for Segmentation Module
 *
 * Retained as an empty shell for backwards compatibility.
 * Session persistence and resume functionality have been removed.
 */

class StateHandler {
  /**
   * @param {SegmentationModule} module - Reference to the parent module
   */
  constructor(module) {
    this.module = module;
  }
}

export default StateHandler;
