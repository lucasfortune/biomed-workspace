# Session — Phase 8: Release 1.3.0

**Date:** 2026-09-04
**Type:** 🚀 Release
**Phase:** 8 (final phase of the workspace consolidation)
**Branch:** `main`
**Status:** ✅ Complete

---

## Goal

Close out the workspace consolidation (Phases 0–7 + the pre-release fix
batch, all already merged to `main`) by cutting release **1.3.0**:
version bump, a hub "What's new" panel, the git tag, and the last docs —
ADR-011 plus the framework/structure references (decisions 2–6, decision 9).

## What shipped

### Version bump
- `package.json` `version` 1.2.2 → **1.3.0**.
- Hub footer (`public/workspace/index.html`) "Workspace Version" 1.2.5 →
  **1.3.0**.

### Hub "What's new" (decision 9)
- A native `<details class="whats-new">` in the footer's `.workspace-info`
  block: summary "What's new in 1.3.0", an 8-bullet user-facing highlight
  list, and a "Full changelog & docs" link to the guides site. No JS.
- `workspace.css` `.whats-new` rules (token-based, both themes).

### Docs
- **ADR-011** — `docs/decisions/011_shared_viewer_chrome_and_conventions.md`
  records decisions 2–6 (toolbar bordered cards + help slot; primary action
  in the nav-row right slot; 1-based `n / N` readouts; reset-on-leave;
  test-data → sample-data seeding) and the shared `SliceViewerChrome` /
  `slice-viewer.css` implementation, with the dead-component removal noted.
- `docs/INDEX.md` — ADR table completed (was stale at 004; now lists
  005–011).
- `docs/reference/FILE_STRUCTURE.md` — `core/` block rewritten to the real
  components (SliceViewerChrome, ValidationDisplay, NavigationButtons,
  ResumeDialog, `icons.js`, `core/css/`) and the module list corrected to
  the 10 modules + template.
- `docs/guides/MODULE_FRAMEWORK.md` — already accurate (Phase 5 rewrite +
  SliceViewerChrome section); verified no dead-component references remain.

### Tag
- `v1.3.0` on the release commit.

## Verification
- `package.json` parses; version reads 1.3.0.
- Hub footer renders the version + collapsible "What's new".
- No references to deleted components in the framework/structure guides.

## Result
The consolidation is released as **1.3.0**. Open tracker items carried
forward: Category E per-module lists (future fix batches).
